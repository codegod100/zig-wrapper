[workspace]

[package]
name = "wry-zig-wrapper"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wry = { path = ".." }
tao = "0.34"

[target.'cfg(target_os = "linux")'.dependencies]
gtk = { version = "0.18", optional = true }
